/*#include <stdlib.h>
#include "libft.h"

char **ft_strsplit(char *s1, char *s2)
{
	char *sbegin, *send;
	static char *ssave = "";

	sbegin = s1 ? s1 : ssave;
	sbegin += ft_strspn(sbegin, s2);
	if (*sbegin == '\0')
	{
		ssave = "";
		return ((void *)0);
	send = sbegin 
	}
}*/
